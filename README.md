# LinuxMintxfceDefaults
The defaults of the Linux Mint xfce.
This are the default files that are created in ~/.config/xfce4/ by Linux Mint when u create a new user/install it. If u want to restore them / to use the Linux Mint xfce defaults, just copy those files in ~/.config/xfce4/. Then restart xfce.
